# Agent Job Coach · 双 AI 评审会议纪要（Codex × Claude）

> 日期：2026-08-28 ｜ 参会：Codex、Claude（Claude Code）｜ 记录：Codex
> 议题：项目实现审视 / GitHub 竞品对标 / 关键技术方案（状态机、题库切块、JD 截图）/ 排期

## ① 现状代码审视（双方一致）

**做得好的**：
- LangGraph 双 Agent 架构方向正确（问答 ReAct 图 + 面试官 interrupt 状态机）
- 分类 RAG（向量+BM25+RRF 混合检索）已达标
- Redis 会话历史 + MySQL 落库的分层清晰

**隐患（按严重度）**：
1. **v1 interrupt 重放 bug（最严重，本轮新发现）**：`ask` 节点"生成题目 + interrupt"混合，LangGraph 恢复时节点重放会重复生成题目 → M2 必须改
2. 模型未切 glm-5.1（llm.py / .env 仍是 DeepSeek）
3. 命名未改（JobPilot 残留：app_name / Chroma collection / Redis key）
4. 非 git 仓库（quality-gate 工作流无法生效）
5. 调试脚本残留（_run_interview.py / _dbg_interview.py）
6. `.env` 有明文密码/API key；config.py 密码硬编码默认值
7. `search.py:_load_corpus` 每次全量扫库，性能隐患
8. 需求文档 129 行仍写 "DeepSeek"
9. 题库文件是"笔记型"（每文件单 h1、无题目标记），当前切块从 500 字符中间切断，语义被破坏

## ② 三项技术决策

### 决策 1：存储分层（修正 1）——单一状态源 = MySQL
- 题单等持久资产入 MySQL（`interview_question_bank` 表：id/session_id/question/category/status/order）
- Redis 只存会话元信息 + 事件流（7 天 TTL）
- LangGraph 图内 state 只放当前一轮临时数据（question/answer/score/feedback）

### 决策 2：图结构（修正 2）——interrupt 独占 wait 节点
```
START → route(op)
 ├─ ask   ：只生成题目写入 state（无 interrupt）
 ├─ wait  ：独占 interrupt，从 state 读 waiting_for 决定 payload（等首答/等追问）
 └─ judge ：评分 + 反馈 + 条件边（deep_dive → ask 生成追问 / 本题结束 → END）
```
关键：重放只重放 wait（幂等），题目不会重复生成——根除 v1 重放 bug。
深挖上限 2-3 轮（deep_dive_round 计数），达到或回答充分即本题结束。

### 决策 3：评分鲁棒性
- `response_format=json_object` 硬约束 + 正则提取兜底 + Pydantic 校验 + 失败重试一次
- 参考答案检索与评分拆开（不塞同一调用）
- **金标评测集需人工标注基线**（自评方差测不出偏差）——从 M0 顺手积累，M3 系统化跑

## ③ 题库双模式切块（M1 实现）
- **笔记型（现状语料）**：语义段落切块 + 中文分隔符，section = 文件名/章节
- **Q&A 型（新支持）**：识别题目边界（题号/题型/知识点元数据）切块，整题入库
- 配套：per-file 幂等重灌 + BM25 语料缓存失效钩子（add/delete 时失效，挂 M1 不单占工期）

## ④ JD 截图解析（F2.0，M1 实现）
- `get_vision_model()`（glm-4v-plus），OpenAI 兼容 content parts 传 base64 data URI
- 4v 的 json_object 不稳 → 走公共 `call_llm_json` helper
- **回显确认不可省**：解析结果先进"草稿"，前端可编辑表单，用户确认后才写 jd_record + Chroma（OCR 错了不污染素材库）
- 图片 ≤5MB + Pillow 压缩控 token；失败降级引导文本粘贴
- 解析单次调用；确认后的定制流水线（诊断→要点→讲稿→题单）走 SSE 分段流式

## ⑤ M0-M4 排期

| 阶段 | 内容 | 周期 |
|---|---|---|
| M0 | git init+首 commit、.gitignore、删调试脚本、**模型切 glm-5.1（三项实测：SSE 流式 / response_format 是否生效 / max_tokens=4096 下评分 JSON 是否被推理链截断）**、代码层改名 Agent Job Coach、归档 legacy_zst | 半天 |
| M1 | 素材库（简历母版/项目档案/面经）+ JD 定制（匹配/简历要点/STAR 讲稿/题单）+ 题库双模式切块器 + BM25 缓存 + JD 截图解析 | 1 天 |
| M2 | 题单化面试状态机（wait 节点图结构）+ 连续深挖 + 当场点评（**核心壁垒**） | 1 天 |
| M3 | 金标评测集系统化（人工标注基线）+ 评分一致性 + 历史对比 | 0.5 天 |
| M4 | 打磨 + README + 简历包装 + 全流程演示 | 0.5 天 |

## ⑥ 待用户裁决清单
1. 语音面试（P2，+2 天）要不要
2. 在线算法 IDE（P2）要不要
3. 多用户登录先不做？
4. 简历/讲稿导出格式：Markdown 先上还是直接 .docx
5. 素材录入方式：表单 / 文件导入 / 对话式引导
6. M0 四项是否批准开工
