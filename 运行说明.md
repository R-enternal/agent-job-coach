# Agent Job Coach（Agent 求职助手）

> 由 ZST 智扫通（扫地机器人客服教学项目）重构而来：
> 场景改为「求职面试陪练」，后端 Flask→FastAPI，Agent 手写 ReAct→LangGraph 多智能体，
> 前端 Streamlit→Vue3，新增面试官 human-in-the-loop 状态机与分类知识库。

## 技术栈

| 层 | 技术 |
|---|---|
| 后端 | FastAPI + uvicorn + sse-starlette |
| Agent | LangGraph 1.x（问答 ReAct 图 + 面试官状态机 + 孤儿 tool_calls 自愈） |
| RAG | 多格式解析 + 结构感知切块 + 分类知识库（简历/项目/题库/JD）+ 向量/BM25/RRF 混合检索 |
| 模型 | 智谱全家桶：`glm-5.1`（对话，max_tokens=4096 容纳推理链）+ `glm-4v-plus`（视觉识图）+ `embedding-2`（向量） |
| 存储 | MySQL（面试记录）+ Redis（会话历史 7 天 TTL） |
| 前端 | Vue3 + Vite + Element Plus |

## 启动

### 1. 后端（端口 9902）

```powershell
cd "D:\AI Program\AI课程-ZST\02-ZST_code\backend"
.\.venv\Scripts\python.exe scripts\seed_kb.py   # 首次：灌知识库（题库/项目文档）
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 9902
```

API 文档：http://127.0.0.1:9902/docs（Swagger）

### 2. 前端（端口 5174）

```powershell
cd "D:\AI Program\AI课程-ZST\02-ZST_code\frontend"
npm run dev
```

浏览器打开 http://localhost:5174

## 功能

1. **知识问答**：SSE 流式对话，Agent 自动选择工具（知识检索/笔记生成/JD 匹配/项目深挖）
2. **模拟面试**：选择主题 → 面试官出题 → 你回答 → 评分 + 反馈 → 下一题 → 复盘报告（MySQL 落库）
3. **项目深挖**：针对仓维云 / Agent Job Coach 生成 5 个深挖追问或 0.5/1/3 分钟介绍话术
4. **JD 匹配**：粘贴岗位 JD → 解析技能要求 → 与简历/项目匹配度诊断 + 优化建议
5. **知识库管理**：`POST /api/kb/upload` 上传简历/JD/项目文档（resume/jd/project/interview 分类）

## 数据目录

```
data/
├── interview_questions/   # 面试题库（从《二面Agent专项准备》解析）
├── projects/              # 项目文档（仓维云 + Agent Job Coach）
├── resume/                # 个人简历（待上传）
└── jd/                    # 岗位 JD（待上传）
```

## 关键实现

- **面试官 Agent**：LangGraph 状态机 + `interrupt`（human-in-the-loop）——图运行到出题节点暂停，
  前端作答后 `Command(resume=...)` 恢复，评判后条件边决定下一题或结束
- **防失控**：问答 Agent 步数上限 6；孤儿 tool_calls 自愈清洗（_sanitize_messages）
- **可溯源**：RAG 回答带来源/章节引用（`【资料 1 | 来源: ... | 章节】`）
- **记忆**：LangGraph checkpointer（MemorySaver）按 thread_id 保存图状态；Redis 持久化会话历史
