# Agent Job Coach（Agent 求职助手）

> 面向求职面试的 LangGraph Agent，后端 FastAPI + 前端 Vue3：
> LangGraph 多智能体（问答 ReAct 图 + 面试官 human-in-the-loop 状态机），
> 内置分类知识库与 RAG 混合检索。

## 技术栈

| 层 | 技术 |
|---|---|
| 后端 | FastAPI + uvicorn + sse-starlette |
| Agent | LangGraph 1.x（问答 ReAct 图 + 面试官状态机 + 孤儿 tool_calls 自愈） |
| RAG | 多格式解析 + 结构感知切块 + 分类知识库（简历/项目/题库/JD）+ 向量/BM25/RRF 混合检索 |
| 模型 | 智谱全家桶：`glm-5.1`（对话，max_tokens=4096 容纳推理链）+ `glm-4v-plus`（视觉识图）+ `embedding-2`（向量） |
| 存储 | MySQL（持久资产）+ SQLite（图状态 checkpoint，断点续答）+ Redis（会话历史/面试事件 7 天 TTL） |
| 前端 | Vue3 + Vite + Tailwind（自研组件库，无第三方 UI 框架） |

## 启动

### 1. 后端（端口 9902）

```powershell
cd "D:\AI Program\Interview agent\agent-job-coach\backend"
.\.venv\Scripts\python.exe scripts\seed_kb.py   # 首次：灌知识库（题库/项目文档）
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 9902
```

API 文档：http://127.0.0.1:9902/docs（Swagger）

### 2. 前端（端口 5174）

```powershell
cd "D:\AI Program\Interview agent\agent-job-coach\frontend"
npm install   # 首次
npm run dev
```

浏览器打开 http://localhost:5174（vite 已代理 /api → 9902）

## 前端页面

| 页面 | 路径 | 功能 |
|------|------|------|
| 首页 | / | 三大功能入口 + 最近面试场次 |
| 知识问答 | /chat | SSE 流式对话，Agent 自动选择工具（知识检索/笔记生成/JD 匹配/项目深挖）；多会话；支持语音输入 |
| 资料库 | /library | 简历上传（LLM 结构化解析入库）+ 知识库资料上传/分类筛选/删除 |
| 模拟面试 | /interview | JD 文本粘贴 / 截图上传双通道解析 → draft 回显编辑 → 确认 → 生成三类题单（八股/项目/业务）；作答（支持语音输入）/挑题/跳过/主动结束；**五维评分** + 深挖追问（≤2 轮）；**三档答案打磨**（30s/1min/2min 双语）；复盘报告（含上场对比）；历史场次点击回放问答明细 |

## 完整使用流程

1. **资料库**：上传简历全文（自动结构化）与面试资料（题库/项目文档/JD）
2. **模拟面试**：粘贴 JD 文本或上传招聘 App 截图 → 核对解析结果 →「确认无误」（仅 confirmed 的 JD 可生成题单）→「生成三类题单」→ 开始面试
3. **作答**：逐题作答（Enter 发送、Shift+Enter 换行），AI 五维评分并给点评，薄弱点深挖追问；支持点击题单挑题、「跳过本题」（不计分但复盘中可见）、主动结束
4. **复盘**：面试结束自动生成复盘报告（含每题综合分与上场对比）；左侧「历史场次」点击任意场次可回看问答明细（明细存 Redis 保留 7 天，过期仅留 MySQL 汇总）

## 数据目录

```
data/
├── interview_questions/   # 面试题库（从《二面Agent专项准备》解析）
├── projects/              # 项目文档（仓维云 + Agent Job Coach）
├── evals/                 # 评分金标集 judge_golden.jsonl + 评测结果
├── resume/                # 个人简历（本地维护，git 已忽略）
└── jd/                    # 岗位 JD（本地维护，git 已忽略）
```

## 关键实现

- **面试官 Agent**：LangGraph 状态机 + `interrupt`（human-in-the-loop）——图运行到 wait 节点暂停，
  前端操作后 `Command(resume={"op":"answer"/"pick"/"skip"})` 恢复；LLM/DB 副作用全在 wait 之前，
  恢复只重放幂等的 wait（杀进程重启可断点续答）
- **防失控**：深挖追问 ≤2 轮代码强制（不信 LLM finished）；问答 Agent 步数上限 6；孤儿 tool_calls 自愈清洗
- **评分双保险**：response_format=json + 正则兜底 + Pydantic clamp(0-10) + 重试 + 降级 5 分显性标记；
  **五维评分**（correctness/depth/structure/expression/risk_awareness）随总分输出并 clamp，
  总分独立按分档锚点定档（严禁取五维平均——防漂移约束经金标集回归验证）
- **结束面试收尾**：`POST /api/interview/end`（resume op=end 直达 END），进行中会话强制生成复盘报告落库；
  零作答场次不落库；已完结场次重入返回 409
- **可溯源**：RAG 回答带来源/章节引用（`【资料 1 | 来源: ... | 章节】`）
- **记忆**：面试图同步 `SqliteSaver`、问答图 `AsyncSqliteSaver`（`backend/checkpoint_db/*.db`，双库防 thread_id 冲突）；
  Redis 持久化会话历史与面试事件流（7 天 TTL）
- **评分校准**：`data/evals/judge_golden.jsonl` 金标集（人工基线 + Codex 初审），
  `uv run python scripts/eval_judge.py --tag <标签>` 跑一致率/MAE/Pearson，
  逐条明细落 `data/evals/judge_eval_result.json`（迭代回归可追溯）；
  实测 baseline 67% → 分档锚点 rubric-v1 100%（MAE 0.75，Pearson 0.99）
- **复盘回放**：`GET /api/interview/history?session_id=xxx` 返回单场事件流（含 skipped 标记），
  前端历史场次点击进入回放弹窗。**计分口径**：场次 `avg_score` = 每题 final（首答50%+追问均分50%）
  的均分（skipped 不计），不按作答事件计分——追问是同题能力暴露的深化，不与首答平权稀释
- **上传安全**：知识库 / JD 截图上传校验分类白名单 + 文件类型 + 大小上限（`MAX_UPLOAD_MB`，默认 10MB），
  文件名只取 basename 防路径穿越
