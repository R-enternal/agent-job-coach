# Agent 求职助手 — 需求文档（v2.0 重构版）

> 版本：v2.0 ｜ 日期：2026-08-28 ｜ 状态：待评审
> 依据：GitHub 同类项目调研（sspOffer / job-hunt-copilot / Lujie-Careerkit / liftoff / ai_mock_interviews / Resume-Matcher 等）

---

## 1. 背景与问题

求职过程高度重复但高度定制化：

1. **简历反复改**：每投一个岗位，措辞、项目顺序、侧重点都要变，手动改效率低、易遗漏。
2. **项目讲法不通用**：同一个项目，投技术岗和投产品岗的讲法完全不同；面试前要口述讲稿，还要能应对追问。
3. **面试没人陪练**：模拟面试缺少"像真人面试官一样连续追问、给出评分"的陪练对象。
4. **复盘无积累**：面完就忘，没有结构化记录，无法对比"这次比上次进步在哪"。

**调研结论**（GitHub 高 star 项目验证）：成熟的求职 AI 产品普遍具备「素材库 → JD 定制 → 模拟陪练 → 复盘对比」四层能力闭环，而不是单一的问答或聊天。

---

## 2. 项目定位

> **面向求职面试的 Agent：个人求职素材库 + JD 驱动定制 + 面试陪练闭环**

一句话：**Agent 求职助手** —— 面向求职面试场景的智能体，先录入个人简历/项目/面经形成素材库，
粘贴 JD 后按岗位定制简历要点、项目讲稿与面试题单，并通过题单化模拟面试（连续深挖 + 评分）
陪练，最终产出整场复盘与历史对比，形成"素材 → 定制 → 陪练 → 复盘"的求职闭环。

> **项目英文名（正式）**：**Agent Job Coach**（简历/README 统一使用）。中文名：Agent 求职助手。

---

## 3. 目标用户与场景

### 3.1 目标用户

- 主用户：应届生 / 实习求职者（以本人求职 Agent 开发岗为第一场景）
- 扩展用户：通用技术求职者

### 3.2 核心使用场景（用户故事）

| # | 用户故事 | 对应模块 |
|---|---------|---------|
| S1 | 作为求职者，我想把简历母版、项目档案、面经一次性录入素材库，之后投岗位不用重复找资料 | 素材库 |
| S2 | 作为求职者，粘贴一个 JD 后，我想得到匹配度诊断 + 定制简历要点 + 项目讲稿 + 面试题单 | JD 定制 |
| S3 | 作为求职者，我想按题单挑题作答，面试官围绕我的回答连续追问，并给出点评和参考答案 | 面试陪练 |
| S4 | 作为求职者，面试结束后我想看到五维评分，并和上次面试对比进步/退步项 | 复盘对比 |
| S5 | 作为求职者，我想随时回看历史面试记录和复盘报告 | 复盘对比 |

---

## 4. 功能需求

### 4.1 模块一：个人素材库（P0）

**目标**：把零散资料结构化，成为定制输出的"原材料"。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F1.1 | 简历母版管理 | 支持录入/编辑多份简历母版（按岗位方向区分，如"Agent 开发"、"数据科学"），Markdown 或 PDF 导入 | P0 |
| F1.2 | 项目档案结构化录入 | 每个项目录入：项目名称 / 我的角色 / 技术栈 / 核心实现 / 量化成果 / 开发故事 / 可追问点 | P0 |
| F1.3 | 面试经验（面经）录入 | 支持文本录入；可选图片 OCR（P2） | P1 |
| F1.4 | 素材统一检索 | 分类 RAG（resume / project / interview / jd），回答可溯源 | P0（已有） |
| F1.5 | 素材库看板 | 展示已录入的素材清单、各分类片段数 | P1 |

### 4.2 模块二：JD 驱动定制（P0）

**目标**：粘贴 JD → 一键产出投递该岗位所需的全部"弹药"。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F2.0 | JD 输入双通道 | 文本粘贴（已有）+ 截图上传：GLM-4V 多模态解析 → 回显结构化 JD → 用户确认/修改后进入定制流程 | P0 |
| F2.1 | JD 结构化解析 | 提取技能要求、职责、经验门槛、软实力 | P0 |
| F2.2 | 匹配度诊断 | 与素材库对比，输出 0-100 分 + 强项/差距/风险清单（升级现有 match_job） | P0（已有雏形） |
| F2.3 | 定制简历要点 | 输出"选哪些项目、排除哪些、每个项目怎么改措辞"（附理由） | P0 |
| F2.4 | 项目讲稿生成 | STAR 结构：开场句 → 背景问题 → 我做了什么 → 结果 → 与岗位连接；每项目附 2-3 条可能追问 | P0 |
| F2.5 | 面试题单生成 | 按 JD + 素材库生成 9-12 道深挖题，覆盖 实习/项目/八股/岗位技术/软技能；SSE 流式逐段生成 | P0 |
| F2.6 | 简历/讲稿导出 | 导出 Markdown（后续可加 .docx） | P1 |

### 4.3 模块三：面试陪练（P0 · 核心）

**目标**：模拟真人面试官，不是一题一换，而是"题单 + 连续深挖 + 当场点评"。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F3.1 | 题单化面试 | 一次生成题单，用户自由挑题作答（而非系统逐题强推） | P0 |
| F3.2 | 连续深挖追问 | 面试官围绕**当前问题**连续追问 2-3 轮（基于用户回答生成 follow-up），减少跑题 | P0 |
| F3.3 | 当场点评 | 每次作答后给出：点评 / 参考答案要点 / 深挖建议（辅助信息，不打断流程） | P0 |
| F3.4 | 面试风格与类型 | 风格：友好复盘 / 高压追问；类型：BQ / JD 面 / 技术面 / 项目深挖 / 混合 | P0 |
| F3.5 | 思考过程流式展示 | SSE 输出"正在生成题目 / 正在评判"等结构化事件 | P0（已有基础） |
| F3.6 | 面试会话持久化 | 中途退出可恢复，状态存 Redis/MySQL | P0 |

### 4.4 模块四：复盘与对比（P1）

**目标**：把一次面试变成可积累的经验资产。

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F4.1 | 整场五维评分 | 正确性 / 深度 / 结构 / 表达 / 风险意识，各 0-10 分 + 总分 | P1 |
| F4.2 | 历史对比分析 | 与历史会话同维度对比，输出进步项 / 退步项 / 稳定项 / 优先加强建议 | P1 |
| F4.3 | 复盘报告 | 结构化报告（已有雏形，升级为含五维 + 对比） | P1 |
| F4.4 | 面试历史列表 | 按时间倒序查看历史场次、主题、均分（已有） | P1 |
| F4.5 | 投递追踪 | 记录投递岗位、状态（投递/笔试/一面/二面/offer），与 JD 记录关联 | P2 |

### 4.5 模块五：知识库管理（P0）

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F5.1 | 分类管理 | resume / project / interview / jd 四类，检索可按分类过滤 | P0（已有） |
| F5.2 | 上传入库 | 支持 md/txt/html/pdf/docx，上传后自动解析切块入库 | P0（已有） |
| F5.3 | 知识库重建 | 一键重建向量库（数据变更后） | P0（已有） |

### 4.6 模块六：系统与可靠性（P0/P1）

| 编号 | 需求 | 说明 | 优先级 |
|------|------|------|--------|
| F6.1 | 会话记忆 | Redis 会话历史 7 天 TTL（已有） | P0 |
| F6.2 | Agent 防失控 | 步数上限 6、孤儿 tool_calls 自愈（已有） | P0 |
| F6.3 | 多轮上下文 | LangGraph checkpointer 按 session 隔离（已有） | P0 |
| F6.4 | 用户登录 | 单用户本地部署可先不做；多用户需登录 + 数据隔离 | P2 |
| F6.5 | API 降级 | LLM 调用失败时给出友好提示与重试建议 | P1 |

---

## 5. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | SSE 首 token ≤ 3s；单次问答 ≤ 15s（智谱 glm-5.1 正常网络下）；题单生成 ≤ 60s |
| 可靠性 | 面试中断可恢复；评分 JSON 解析失败有兜底默认值；知识库检索失败回退纯向量 |
| 隐私 | 简历/项目数据本地存储（MySQL/Redis/Chroma 均本机）；不上传第三方（除 LLM API 调用） |
| 可维护性 | 模块分层（api/agent/rag/tools/services）；配置走 .env；README 与需求文档齐备 |
| 可观测 | 面试/工具调用留痕（MySQL + Redis），便于复盘与排查 |
| 兼容 | 前端支持 Chrome/Edge 最新版 |

---

## 6. 技术架构

### 6.1 现状（v1 已完成）

```
Vue3(5174) → FastAPI(9902) → LangGraph 多智能体 → 工具层 → 分类 RAG（Chroma+BM25+RRF）
                            → MySQL（面试记录）→ Redis（会话记忆）
```

- 问答 Agent：ReAct 图（agent/tools 节点 + 条件边 + 步数上限）
- 面试官 Agent：interrupt 状态机（出题 → 作答 → 评判 → 下一题/结束）
- 工具：query_knowledge / match_job / gen_study_notes / dig_project / project_intro
- 模型：智谱全家桶（OpenAI 兼容）——对话 `glm-5.1`（max_tokens 调大至 4096 容纳推理链）、视觉 `glm-4v-plus`（JD 截图解析）、向量 `embedding-2`

### 6.2 需要新增/改造（v2）

| 改造点 | 说明 |
|--------|------|
| 素材库数据模型 | MySQL 新增 `resume_base` / `project_archive` / `jd_record` 表 |
| JD 定制流水线 | 新工具：`generate_resume_points` / `generate_project_script` / `generate_question_bank` |
| 题单化面试 | 面试状态机升级：题单状态（remaining/answered）+ 自由挑题 API |
| 连续深挖 | 面试官 judge 节点增加"基于当前回答生成 follow-up"分支 |
| 五维评分 | 评分 prompt 升级为 JSON 结构化输出（5 维 + 总分） |
| 历史对比 | 新增对比服务：读取历史场次 → LLM 生成对比报告 |
| 前端 | 新增"素材库"页；面试页改为题单模式（挑题 + 深挖轮次展示） |

---

## 7. 数据模型设计

### 7.1 已有表

| 表 | 字段（摘要） |
|----|-------------|
| `interview_records` | session_id, topic, rounds, avg_score, summary |
| `answer_records` | session_id, round, question, answer, feedback, score |
| `kb_documents` | filename, category, chunk_count |

### 7.2 新增表（v2）

| 表 | 字段（摘要） | 用途 |
|----|-------------|------|
| `resume_base` | id, title(方向), content_md, version, updated_at | 简历母版多版本 |
| `project_archive` | id, name, role, tech_stack, highlights, story, quant_results, followups | 项目档案结构化 |
| `jd_record` | id, company, title, raw_text, parsed_json, match_score, created_at | JD 记录与匹配结果 |
| `interview_question_bank` | id, session_id, question, category, status(待答/已答/跳过), order | 题单 |
| `interview_feedback` | id, session_id, round, question, answer, comment, reference, deep_dive | 当场点评（扩展 answer_records） |

> 素材内容同时写入 Chroma（向量检索）与 MySQL（结构化展示），两边通过 `source` 关联。

---

## 8. 里程碑与验收

### M1：素材库 + JD 定制（预计 1 天）

- 验收：录入简历母版 + 项目档案后，粘贴 JD 能生成 匹配诊断 + 定制简历要点 + 项目讲稿 + 面试题单

### M2：题单化面试陪练（预计 1 天）

- 验收：生成题单 → 挑题作答 → 面试官连续深挖 2-3 轮 → 当场点评/参考答案；中途退出可恢复

### M3：复盘对比（预计 0.5 天）

- 验收：两场以上面试后，可查看五维评分与历史对比报告（进步/退步项）

### M4：打磨交付（预计 0.5 天）

- 验收：全流程演示脚本 + 评测集（题单质量/检索召回）+ README + 简历包装更新

---

## 9. 竞品对标（调研摘要）

| 项目 | Stars | 可借鉴点 |
|------|-------|---------|
| sspOffer-interview-assistant | 91 | 题单生成 + 连续深挖 + 整场五维评分 + 历史对比（**最直接对标**） |
| job-hunt-copilot | 219 | 素材库模式 + 项目讲稿（STAR + 可能追问）+ 高压/友好两种面试风格 |
| Lujie-Careerkit | 319 | 简历库多版本 + 求职全流程闭环 + 数据看板 |
| liftoff | 1523 | 语音面试（Whisper + 流式反馈，P2 可借鉴） |
| ai_mock_interviews | 567 | 面试历史 Dashboard 管理 |
| jlifeng/JobPilot | 126 | tool_use 精确编辑、多格式导入、隐私导出（已改名 Agent Job Coach 避开撞名） |

---

## 10. 待确认问题

| # | 问题 | 影响 |
|---|------|------|
| ~~Q1~~ | ✅ 已确认：项目命名为 **Agent Job Coach（Agent 求职助手）** | 已解决 |
| Q2 | 是否需要语音面试（录音 → 转写 → 反馈） | 工作量 +2 天，P2 |
| Q3 | 是否需要在线算法 IDE / 算法题库 | 工作量 +2 天，P2 |
| Q4 | 是否需要多用户登录 | 本地单用户可先不做 |
| Q5 | 简历/讲稿导出格式：先 Markdown 还是直接 .docx | 影响交付物 |
| Q6 | 素材录入方式：手动表单填写 vs 文件导入 vs 对话式引导录入 | 影响交互设计 |

---

*文档结束。评审通过后按 M1→M4 迭代开发，一步一验收。*
