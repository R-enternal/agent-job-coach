# 项目状态快照（Codex / Claude 协作恢复用）

> 用途：上下文压缩或会话切换后，双方先读此文件恢复状态。
> 最后更新：2026-08-31（本地）

## 项目
- **Agent Job Coach（Agent 求职助手）**：面向求职面试的 LangGraph Agent，"素材库 → JD 定制 → 陪练 → 复盘"闭环
- 路径：`D:\AI Program\Interview agent\agent-job-coach`（backend FastAPI 9902 / frontend Vue3 5174）
- 技术栈：FastAPI + Vue3（Tailwind 自研组件，无 Element Plus）+ MySQL + Redis + LangGraph + Chroma + GLM 全家桶（glm-5.1 对话 / glm-4v-plus 识图 / embedding-2 向量，OpenAI 兼容接口）

## 协作模式
- 工作流：**Claude 写代码 + Codex 审查**；分步开发、一步一验收；commit/merge 前过 quality-gate
- Claude 原始会话：`7a1e71fe`（标题"Agent 求职助手项目交接与协作"），**禁止新建会话替代**
- 双客户端注意：Codex 走后台 CLI 给 Claude 发消息，用户走 VSCode 面板；两边写同一会话文件，但界面不跨客户端同步 → **AI↔AI 通信尽量落盘文件**

## 里程碑状态
- M0 基建（SqliteSaver 双库 checkpoint、断点续答、interrupt 重放修复）✅
- M1 素材库 + JD 定制（四表、截图 glm-4v-plus 解析、draft→confirmed）✅
- M2 题单化面试状态机（/pick /skip、首答50%+追问50% 计分）✅
- M3 评测金标集（12 条，一致率 67%→100%，MAE 0.75→0.65，Pearson 0.99）✅
- M4/M5 打磨：前端 React→Vue3 重写（commit `89e50fe`）、命名统一 Agent Job Coach ✅
- 最新批次（commit `456503d` 起）：kb 上传路径穿越修复 + 大小限制（MAX_UPLOAD_MB=10）、
  清理仓维云残留文件、删除 /compare 端点（保留复盘报告内"与上场对比"）、
  前端接入 /history 单场回放、中文输入法 isComposing 修复、请求失败输入回填

## git
- 本地仓库，**无 remote，未推送**；用户 GitHub：`https://github.com/R-enternal`（仓库名/是否新建待确认）
- 推 GitHub 前注意：`data/resume/`、`data/jd/`、`backend/.env` 已 gitignore；`会议纪要.md` 含协作内部细节，斟酌是否公开

## 简历定稿口径（用户已拍板）
- 技术栈行：Python | LangGraph | FastAPI | Vue3 | MySQL | SqliteSaver | Chroma | RAG
- 第 3 条：集成 SqliteSaver 实现面试状态持久化，支持中断后断点续答；MySQL 沉淀题单/作答/场次
- 五重评分防护：用户已同意**从简历删除**（留作面试弹药）；自研 Redis 方案已否
- 成果区数字：评分一致率 67%→100%（MAE 0.65，Pearson 0.99）已实测，可回填

## 环境备注
- 本机 Redis 5.0.14 无 RediSearch 模块 → 官方 RedisSaver 不可用，故用 SqliteSaver（`backend/checkpoint_db/`）
- MySQL localhost:3306，密钥与密码在 `backend/.env`（不入库）
