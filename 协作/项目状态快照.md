# 项目状态快照（Codex / Claude 协作恢复用）

> 用途：上下文压缩或会话切换后，双方先读此文件恢复状态。
> 最后更新：2026-08-28 21:20（本地）

## 项目
- **Agent Job Coach（Agent 求职助手）**：面向求职面试的 LangGraph Agent，"素材库 → JD 定制 → 陪练 → 复盘"闭环
- 路径：`D:\AI Program\AI课程-ZST\agent-job-coach`（backend FastAPI 9902 / frontend React 5174）
- 技术栈：FastAPI + React + MySQL + Redis + LangGraph + Chroma + GLM 全家桶（glm-5.1 对话 / glm-4v-plus 识图 / embedding-2 向量，OpenAI 兼容接口）

## 协作模式
- 工作流：**Claude 写代码 + Codex 审查**；分步开发、一步一验收；commit/merge 前过 quality-gate
- Claude 原始会话：`7a1e71fe`（标题"Agent 求职助手项目交接与协作"），**禁止新建会话替代**
- 双客户端注意：Codex 走后台 CLI（PID 27088）给 Claude 发消息，用户走 VSCode 面板（PID 23676）；两边写同一会话文件，但界面不跨客户端同步 → **AI↔AI 通信尽量落盘文件**

## 已完成（M0 收尾大部分）
- git 本地首存 commit `7e5608a`（**无 remote，未推送**）
- 用户 GitHub：`https://github.com/R-enternal`（2026-08-28 用户提供；具体仓库名/是否新建待确认；本机 gh 未登录，git 全局 credential.helper=manager）
- 模型全切 GLM（v1 曾用 DeepSeek）
- interrupt 重放 bug 修复：ask 只生成 → wait 独占 interrupt → judge；深挖轮次代码上限 ≤2
- 命名统一为 Agent Job Coach
- BM25 缓存、密钥环境变量化、旧代码归档、.gitignore

## 当前任务（进行中）
- **M2 题单化面试状态机**（进行中）：题单接图、/pick /skip 挑题、计分规则（首答 50% + 追问均分 50%）

## 已完成（2026-08-28 夜）
- M0 收尾：SqliteSaver 落地（面试图同步 / QA 图 Async，断线续答实测通过）；interview 端点 async def → def 防阻塞
- M1 素材库 + JD 定制：resume_bases/project_archives/jd_entries(含 draft/confirmed)/question_lists 四表；9 端点；JD 文本(glm-5.1)+截图(glm-4v-plus) 双通道解析→draft→PUT 确认；题单配额生成+_kb_fill 兜底；全链路实测通过（截图 OCR 一字不差、JD 定制题真实生效）
- 遗留小项：_TOPIC_NAMES 上移公共常量（M2 开头处理）

## 简历定稿口径（用户已拍板）
- 技术栈行：Python | LangGraph | FastAPI | Vue3 | MySQL | SqliteSaver | Chroma | RAG
- 第 3 条：集成 SqliteSaver 实现面试状态持久化，支持中断后断点续答；MySQL 沉淀题单/作答/场次
- 五重评分防护：用户已同意**从简历删除**（留作面试弹药）；自研 Redis 方案已否
- 成果区 XX% 数字：等 M3 金标集实测后回填（评分一致率、复测提升）

## 排期
- M1：素材库 + JD 定制（resume_base/project_archive 表、glm-4v-plus JD 截图解析、双模式切块已就绪）
- M2：题单化面试（状态机方案已定）
- M3：评测金标集 + 量化数字 → 回填简历
- M4：打磨

## 环境备注
- 本机 Redis 5.0.14 无 RediSearch 模块 → 官方 RedisSaver 不可用，故改 SqliteSaver
- MySQL localhost:3306（密码见 AGENTS.md 环境信息）
